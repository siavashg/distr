# Distr

## Requirements

Requirements:
pkg-config, libevent

## Installing

### OS X (with MacPorts)
    sudo port install pkg-config libevent
    make

### Debian
    sudo apt-get install pkg-config libevent-dev
    make
